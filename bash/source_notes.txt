https://unix.stackexchange.com/questions/4650/determining-path-to-sourced-shell-script

[[ $0 != $BASH_SOURCE ]] && echo "Script is being sourced" || echo "Script is being run"

# Nisu isti - $BASH_SOURCE je trenutno, $0 je skripta koja je zapocela prvi source
echo "source_current $BASH_SOURCE"
echo "source_original $0"

source "$(dirname "$BASH_SOURCE")/..."

Noninteractive source-ing:
https://stackoverflow.com/questions/61886517/bash-export-function-for-other-script-non-interactive
/etc/environment
